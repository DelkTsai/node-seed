import {Component} from '@angular/core';

@Component({
  selector: 'my-app',
  templateUrl: '_build/components/app.html'
})

export class AppComponent {
  title = 'Angular 2 Seed';
  subtitle = 'Node : Jade : Sass : BrowserSync : Socket.io'
}
